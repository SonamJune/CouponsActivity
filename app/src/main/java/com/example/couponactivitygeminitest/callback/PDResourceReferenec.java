package com.example.couponactivitygeminitest.callback;

import android.media.MediaPlayer;
import android.view.View;

public interface PDResourceReferenec {
    void setMediaResources(View tvPlay, MediaPlayer mediaPlayer, boolean scrollPaused);
}
